pipeline {
  agent any

  stages {

    stage('Checkout') {                // 1–9
      steps {
        echo 'Checkout ho raha hai'
      }
    }

    stage('Syntax Check') {             // 10–13
      steps {
        echo 'Pipeline syntax OK'
      }
    }

  }
}

